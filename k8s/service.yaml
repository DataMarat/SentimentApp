apiVersion: v1
kind: Service
metadata:
  name: sentiment-app-service
  namespace: sentiment-app
spec:
  type: LoadBalancer
  selector:
    app: sentiment-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
